"""Framework integration tests for Keyoku SDK.

These tests require the actual frameworks to be installed:
    pip install keyoku[langchain,llamaindex,crewai]

And a running Keyoku API:
    cd api && docker compose up -d
"""
